import {useState} from 'react';import {useForm} from 'react-hook-form';import api,{apiError} from '../api';import {AuthCard,Field} from './Login'
export default function RecoverPassword(){const [message,setMessage]=useState('');const {register,handleSubmit,formState:{isSubmitting}}=useForm<{email:string}>();async function submit(values:{email:string}){try{const {data}=await api.post('/auth/password-reset/',values);setMessage(data.detail)}catch(e){setMessage(apiError(e))}}return <AuthCard title="Recupera tu acceso" subtitle="Te enviaremos un enlace si encontramos tu correo."><form onSubmit={handleSubmit(submit)}><Field label="Correo"><input required type="email" className="form-control" {...register('email')}/></Field>{message&&<div className="alert alert-info">{message}</div>}<button disabled={isSubmitting} className="btn btn-primary w-100">Enviar instrucciones</button></form></AuthCard>}

