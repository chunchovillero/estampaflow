import {useState} from 'react';import {useQuery,useQueryClient} from '@tanstack/react-query';import {useForm} from 'react-hook-form';import api,{apiError} from '../api';import type {Category,Product} from '../types';import {clp} from '../utils';import {Empty,Input,PageHead} from './Customers'
type Form={name:string;category:number;short_description:string;internal_cost:string;sale_price:string;price_type:string;minimum_quantity:number;production_days:number;stock:number;track_stock:boolean;is_public:boolean;variant_name:string;variant_color:string;variant_size:string;variant_material:string}
export default function Products(){const client=useQueryClient();const [open,setOpen]=useState(false);const [categoryName,setCategoryName]=useState('');const [error,setError]=useState('');const {register,handleSubmit,reset}=useForm<Form>({defaultValues:{price_type:'fixed',minimum_quantity:1,production_days:1,stock:0}});const products=useQuery({queryKey:['products'],queryFn:async()=>{const {data}=await api.get('/products/');return data.results as Product[]}});const categories=useQuery({queryKey:['categories'],queryFn:async()=>{const {data}=await api.get('/categories/');return data.results as Category[]}});async function addCategory(){if(!categoryName)return;await api.post('/categories/',{name:categoryName});setCategoryName('');client.invalidateQueries({queryKey:['categories']})}async function save(values:Form){const {variant_name,variant_color,variant_size,variant_material,...product}=values;const variants=variant_name?[{name:variant_name,attributes:{color:variant_color,talla:variant_size,material:variant_material},sku:'',price_adjustment:'0',stock:0,is_active:true}]:[];try{await api.post('/products/',{...product,variants});reset();setOpen(false);client.invalidateQueries({queryKey:['products']})}catch(e){setError(apiError(e))}}return <><PageHead title="Productos" text="Tu catálogo interno de productos y precios." action={()=>setOpen(!open)} label="Nuevo producto"/><div className="panel category-bar"><strong>Categorías</strong><div><input className="form-control" placeholder="Nueva categoría" value={categoryName} onChange={e=>setCategoryName(e.target.value)}/><button className="btn btn-outline-dark" onClick={addCategory}>Agregar</button></div></div>{open&&<form className="panel mt-3" onSubmit={handleSubmit(save)}><div className="row g-3"><Input label="Nombre *" required reg={register('name')}/><div className="col-md-6"><label className="form-label w-100">Categoría *<select required className="form-select" {...register('category',{valueAsNumber:true})}><option value="">Seleccionar</option>{categories.data?.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label></div><Input label="Costo interno" type="number" reg={register('internal_cost')}/><Input label="Precio de venta *" required type="number" reg={register('sale_price')}/><Input label="Cantidad mínima" type="number" reg={register('minimum_quantity',{valueAsNumber:true})}/><Input label="Días de producción" type="number" reg={register('production_days',{valueAsNumber:true})}/></div><label className="form-label w-100 mt-2">Descripción breve<input className="form-control" {...register('short_description')}/></label><h2 className="h6 mt-4">Primera variante (opcional)</h2><div className="row g-3"><Input label="Nombre de variante" reg={register('variant_name')}/><Input label="Color" reg={register('variant_color')}/><Input label="Talla o medida" reg={register('variant_size')}/><Input label="Material" reg={register('variant_material')}/></div><div className="d-flex gap-4 mt-2"><label><input type="checkbox" {...register('track_stock')}/> Controlar stock</label><label><input type="checkbox" {...register('is_public')}/> Publicar más adelante</label></div>{error&&<div className="alert alert-danger mt-3">{error}</div>}<button className="btn btn-primary mt-3">Guardar producto</button></form>}<div className="product-grid mt-4">{products.data?.map(p=><article className="product-card" key={p.id}><div className="product-placeholder"><i className="bi bi-image"/></div><div><small>{p.category_name}</small><h3>{p.name}</h3><p>{p.short_description||'Sin descripción'}</p>{p.variants.length>0&&<small>{p.variants.length} variante(s)</small>}<div><strong>{clp(p.sale_price)}</strong><span>{p.production_days} día(s)</span></div></div></article>)}</div>{products.data?.length===0&&<Empty text="Agrega categorías y luego crea tu primer producto."/>}</>}
