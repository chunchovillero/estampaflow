import {useState} from 'react';import {useQuery,useQueryClient} from '@tanstack/react-query';import {useForm} from 'react-hook-form';import api,{apiError} from '../api';import {clp,chileDate} from '../utils';import {PageHead} from './Customers'
type Request={id:number;title:string;category:string;quantity:number;region:string;commune:string;required_date:string;description:string;match_score:number};type Proposal={id:number;request:number;request_title:string;total_price:string;status:string}
type Form={request:number;total_price:string;unit_price:string;discount:string;shipping_cost:string;production_days:number;estimated_delivery:string;valid_until:string;deposit_percentage:number;technique:string;materials:string;payment_terms:string;comments:string}
export default function Quotes(){const client=useQueryClient();const [selected,setSelected]=useState<Request|null>(null);const [error,setError]=useState('');const {register,handleSubmit,reset}=useForm<Form>({defaultValues:{discount:'0',shipping_cost:'0',deposit_percentage:50}});const requests=useQuery({queryKey:['quote-requests'],queryFn:async()=>{const {data}=await api.get('/quote-requests/');return data.results as Request[]}});const proposals=useQuery({queryKey:['quote-proposals'],queryFn:async()=>{const {data}=await api.get('/quote-proposals/');return data.results as Proposal[]}});async function save(v:Form){try{await api.post('/quote-proposals/',{...v,request:selected?.id,status:'sent'});reset();setSelected(null);client.invalidateQueries({queryKey:['quote-proposals']})}catch(e){setError(apiError(e))}}return <><PageHead title="Cotizaciones" text="Solicitudes compatibles con tu taller."/>{selected&&<form className="panel" onSubmit={handleSubmit(save)}><h2 className="h5">Propuesta para {selected.title}</h2><div className="request-grid"><F label="Precio unitario" type="number" reg={register('unit_price',{required:true})}/><F label="Total" type="number" reg={register('total_price',{required:true})}/><F label="Descuento" type="number" reg={register('discount')}/><F label="Despacho" type="number" reg={register('shipping_cost')}/><F label="Días de producción" type="number" reg={register('production_days',{valueAsNumber:true,required:true})}/><F label="Entrega estimada" type="date" reg={register('estimated_delivery')}/><F label="Válida hasta" type="date" reg={register('valid_until',{required:true})}/><F label="Porcentaje de abono" type="number" reg={register('deposit_percentage',{valueAsNumber:true})}/><F label="Técnica" reg={register('technique')}/><F label="Materiales" reg={register('materials')}/></div><F label="Condiciones de pago" reg={register('payment_terms')}/><F label="Comentarios" reg={register('comments')}/>{error&&<div className="alert alert-danger">{error}</div>}<button className="btn btn-primary mt-3">Enviar propuesta</button></form>}<div className="quote-cards">{requests.data?.map(r=><article key={r.id}><span>{r.category} · Coincidencia {r.match_score}</span><h2>{r.title}</h2><p>{r.description}</p><small>{r.quantity} unidades · {r.commune}, {r.region} · {chileDate(r.required_date)}</small>{!proposals.data?.some(p=>p.request===r.id)&&<button className="btn btn-outline-primary" onClick={()=>setSelected(r)}>Preparar propuesta</button>}</article>)}</div></>}
function F({label,reg,type='text'}:{label:string;reg:object;type?:string}){return <label>{label}<input className="form-control" type={type} {...reg}/></label>}
